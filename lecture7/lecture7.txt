DOM Manipulation
//Attributes
• getAttribute( attr ) Ilto get the attribute value
• setAttribute( attr, value ) I/to set the attribute val th
//Style
• node.style

Insert Elements             let el = document. createElement("div")
• node.append( el) Iladds at the end of node (inside)
• node.prepend(el ) Iladds at the start of node (inside)
• node.before(el ) Iladds before the node (outside)
• node.after(el) ladds after the node (outside)
Delete Element
• node.remove() //removes the node


Homework:
read about appendchild(), and also about removechild().